global:
  scrape_interval: 10s

scrape_configs:
  - job_name: tutum-backend
    static_configs:
      # host.docker.internal: Docker 컨테이너에서 호스트(Windows) localhost 접근
      - targets: ["host.docker.internal:8000"]
    metrics_path: /metrics
