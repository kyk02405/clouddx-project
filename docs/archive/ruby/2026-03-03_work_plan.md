# 2026-03-03 작업 계획 및 진행 현황 (Ruby)

## 오늘 목표
1. `tutum.my/admin` 프론트 + Grafana 대시보드 연동 완성
2. `asset/portfolio`, `asset/charts` 프론트 정리 및 API 연결
3. MinIO / SES / OCR 통합 QA
4. Backup 계획 및 Canary CI/CD 적용 여부 점검
5. 전체 프로젝트 점검(담당 영역 외 포함)

---

## 진행 현황

### 1) Admin-Grafana 연동
- 상태: `완료`
- 결과:
  - Grafana dashboard 연동 페이지 정리
  - UID/패널/베이스 URL 환경변수 기반 구성 반영

### 2) Asset/Portfolio + Charts
- 상태: `진행 중`
- 결과:
  - 차트 페이지 mock 제거
  - watchlist/보유자산 기반 종목 구성으로 전환
  - 실시간 가격 API 연결 개선
- 남은 작업:
  - `/portfolio/asset` 화면 정리
  - 실패/빈 데이터 fallback UX 정리

### 3) MinIO/SES/OCR QA
- 상태: `진행 중`
- 확인 사항:
  - MinIO endpoint는 내부 DNS(`minio.tutum-storage.svc.cluster.local:9000`)가 정상
  - worker 노드에서 `kubectl localhost:8080 refused`는 Cloudflare 이슈가 아니라 kubeconfig/context 이슈
  - OCR 호출 기준 정리 필요(별도 OCR 서비스 경로 사용)
- 조치:
  - QA 문서를 실환경 기준으로 전면 수정
  - `docs/plans/infra/RUBY_MINIO_SES_OCR_K8S_QA.md`

### 4) Backup / Canary
- 상태: `미완료`
- 다음 작업:
  - 백업 정책(데이터/레지스트리/매니페스트) 표준화
  - canary 단계(10% -> 30% -> 100%) 적용 설계

### 5) 전체 점검
- 상태: `미완료`
- 다음 작업:
  - 뉴스 파이프라인, OAuth, OCR, 시세 API, 알림 흐름 재검증
  - 운영 문서와 실제 배포 설정 정합성 점검

---

## AWS Migration 후속 문서
- 상태: `기준 문서 동기화 완료`
- 파일:
  - `docs/plans/infra/AWS_MIGRATION_PLAN_2026-03-03.md`
- 반영 내용:
  - EKS 사용 + ECR 전환 확정
  - Harbor 미사용, GitLab 단일 운영(소스/CI/CD) + AWS 배포는 ECR 고정
  - Session Manager(SSH 미사용), LGTM 전용 EC2 분리, MinIO -> S3 -> Glacier 정책 반영
  - 비용/단계/리스크/Done 기준 재정의

